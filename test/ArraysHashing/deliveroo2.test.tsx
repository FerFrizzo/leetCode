import { weatherStation } from "../../src/ArraysHashing/deliveroo2"

describe("Deliveroo 2", () => {
  it("should whatever the test", () => {
    const result = weatherStation('all');
    expect(result).toBe(true);
  });


});
